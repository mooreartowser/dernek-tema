# Components Layer

Store reusable components and UI segments (e.g., buttons, donation widgets, activity cards, alerts) here.

## Benefits:
- Promotes modular and DRY (Don't Repeat Yourself) HTML design.
- Components can be dynamically loaded using custom helpers.

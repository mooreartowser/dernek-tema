# WordPress Template Parts

This directory is for standard WordPress template parts. They are designed to be loaded via the native `get_template_part()` function.

## Examples:
- `content-single.php` (Individual post body markup)
- `content-page.php` (Page body markup)
- `content-none.php` (No posts found message markup)

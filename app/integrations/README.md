# Integrations Layer

This folder is dedicated to external integrations such as CRM tools (e.g., Salesforce, Fonzip), analytics modules, payment gateways, and mailing APIs.

## Purpose:
- Isolate external client connections from core WordPress operations.
- Allow swapping CRM providers easily without modifying the theme's templates or core setup files.
- Files here are automatically loaded during bootstrap.
